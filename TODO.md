# mini-hot-hub 待办清单（伏笔汇总）

> 由 `MEMORY.md`「待办 / 伏笔」节整理生成 · 最后更新：2026-08-19  
> 用途：后续「问题汇总、逐个解决」的跟踪文档。

## 状态说明

- ⬜ 待处理（未开始）
- 🔵 方向已提出，方案待设计 / 拍板
- ✅ 已确定 / 已完成

---

## 一、待办项（开放）

### TODO-1 分类栏增加「实习」分类（方向性）

- **归属伏笔**：伏笔-分类栏「实习」
- **现状**：方向性想法，**尚未设计、未写代码**。
- **待办内容**：未来页面考虑增加「分类栏」（按内容主题切换，区别于现有按平台聚合的三卡），候选分类含「实习 / 招聘 / 校招」等。
- **约束 / 注意**：与现有「三平台真实抓取」架构无冲突；落地需新数据源 / 分类维度，评估后再做。
- **状态**：🔵 方向已提出，方案待设计

### TODO-2 手机端访问改为「网址登录」（替代局域网登录）

- **归属伏笔**：伏笔-真机访问方式
- **现状**：当前手机调试靠 Vite `--host` 暴露**局域网 IP**（如 `http://192.168.30.186:5174`），依赖同 Wi-Fi + 防火墙放行 node/vite 入站，可分享性差、换网络即失效。
- **待办内容**：后续将手机端访问方式从「局域网登录」改为「网址登录」——改为通过**公网网址 / 域名**访问（如部署到云托管 / 静态站点 / CDN，或内网穿透拿到公网 URL），手机用普通网址打开，不再依赖局域网。
- **约束 / 注意**：与现有前端架构无冲突；落地需先选部署方案（参考 `MEMORY.md`「用户环境踩坑 · 真机调试」节）。
- **状态**：🔵 方向已提出，方案待设计

### TODO-3 桌面奶黄气泡位置可移动（拖拽 / 可调）

- **归属伏笔**：新增（由「气泡到位」后顺延的增强需求）
- **现状**：当前气泡已按**方案②（右上溢出 `right:-180px`，盖向右侧其他列）**定稿，位置为**固定锚定**——由 `.hot-card__bubble` 的 `right / bottom` 写死。
- **待办内容**：后续希望气泡位置**可移动**（如鼠标拖拽调整、或提供几个预设位可选），让用户按自身习惯摆放，把体验交还给人，契合定位「将心注入」。
- **候选方向（待拍板）**：
  - 拖拽：hover 时可直接拖动气泡到任意位置（需记录拖拽偏移，可考虑 localStorage 持久化）；
  - 预设位：提供「右上 / 正上 / 下方 / 跟随鼠标」等选项切换；
  - 仅桌面端（≥720px）生效，移动端仍 `display:none`、不参与。
- **约束 / 注意**：不能破坏现有「溢出盖其他列、不挡自己表头」的既定行为基线；改动只限 `HotCard.css` + 必要的 TS 状态（如拖拽偏移量），后端无需动。
- **状态**：🔵 方向已提出，方案待设计

### TODO-4 主题色切换（暖黄 / 浅绿 / 深黑）

- **归属伏笔**：新增（用户 2026-08-19 提出）
- **现状**：当前仅有「暖黄」一套主题（即已落地的全站暖奶油色系，作为默认）；尚未有任何切换机制，色值集中在 `App.css` / `index.css` 的 `:root` 变量族。
- **待办内容**：提供用户可切换的「主题色」功能，预设三套：① **暖黄**（当前默认，契合 PRD 温暖定位）；② **浅绿**；③ **深黑**（暗色）。三套**本质上都是对眼睛友好的颜色**。
- **候选方向（待拍板）**：
  - 机制：把现有着色抽成 CSS 自定义属性（变量族），三套主题各一组变量值；通过给 `<html>`/`<body>` 加 `data-theme="warm|green|dark"`（或在 `:root` 切换变量）实现整站换肤；
  - 入口：页脚 / 设置区放一个克制的主题切换器（如三个小图标：暖阳 / 新芽 / 月亮），点击即换；
  - 持久化：localStorage 记住选择，下次访问自动恢复。
- **约束 / 注意**：
  - 暖黄保持默认、为 PRD「温暖、柔软、低饱和、有呼吸感」基调的锚点；浅绿 / 深黑为「护眼」诉求的用户自选变体；
  - **守 P5 包容**：无论哪套，正文对比度须达标、字号看清，不可因暗色牺牲可读性；
  - 既已写死的视觉（桌面奶黄气泡、品牌色 chip、悬停反馈）须随主题联动（如深黑下改用对应描边/底色），避免某套主题下元素「失联」；
  - 改动只限前端 CSS 变量 + 轻量 TS 切换逻辑，后端无需动。
- **✅ 已落地（2026-08-20）**：用户要求主题切换**不单独罗列**，而是收进「设置」里（与浏览垃圾桶/投喂金句等未来功能共用一个设置中枢）。实现：
  - **设置入口**：右上角「刷新」旁新增齿轮按钮（`.home-settings`，描边随主题色）；点击弹出 `SettingsPanel`（底部弹出卡片 / 桌面居中对话框）。
  - **主题三选**：暖黄 / 浅绿 / 深黑，呈现为三个小图标卡（暖阳☀ / 新芽🌿 / 月亮🌙 内联 SVG，无 emoji），当前项暖金描边高亮；点击即时切换、整站 0.3s 柔和过渡。
  - **持久化**：`localStorage['mini-hot-hub:theme']`（warm/green/dark），刷新/重开保持；`index.html` 内联脚本在首屏绘制前应用，避免主题闪烁（FOUC）。
  - **主题驱动**：全站配色从写死改为变量族（`App.css` 的 `:root` 暖黄默认 + `:root[data-theme=green/dark]` 覆盖），名次圆牌 `--rank-bg/fg/spot`、气泡、骨架屏、Tab 交互态、入场动画渐变、错误/爱色全部随主题联动；平台品牌 chip 保持原色（小面积点缀）。
  - **预留位**：设置面板「更多」区已为 TODO-5（浏览垃圾桶）/ TODO-8（投喂金句）留好结构（标「规划中」），届时直接填功能。
  - 构建通过（CSS 14.61kB），已 CloudStudio 部署上线（同沙盒 191d2bb… 覆盖）；手机端三主题 + 刷新保持已可验。
- **状态**：✅ 已确定 / 已完成（已上线，主题切换在「设置」内）

### TODO-5 浏览垃圾桶（已读移入 + 浏览记录）

- **归属伏笔**：新增（用户 2026-08-19 提出）
- **现状**：当前列表展示全部热榜条目，点开链接后标题仍留在原列表，无「已读 / 记录」概念；纯前端展示，数据来自后端实时抓取。
- **待办内容**：新增「浏览垃圾桶」功能——用户**看过（点击打开）**的标题自动**移入垃圾桶**，不再占据主列表位置；垃圾桶本身兼具「浏览记录」能力，可回顾已看过的条目。
- **候选方向（待拍板）**：
  - 「看过」的定义：点击标题链接（新标签页打开）即记为已读；或 hover/停留也算（需拍板）；
  - 存储：localStorage 记录已读标识（建议用 `source + rank` 或标题文本做 key），跨刷新保留；
  - 主列表过滤：已读项从三张卡主列表隐藏，仅出现在垃圾桶；
  - 垃圾桶 UI：一个入口（图标 + 计数），展开显示已读条目列表，支持「还原到列表 / 清空记录」；
  - 浏览记录感：垃圾桶内条目可显示「何时看过」或保留原链接，方便回看。
- **约束 / 注意**：
  - 仅前端展示层过滤，**不改动后端抓取与真实数据契约**，不违反「严格不回退」；
  - 已读状态是本地偏好，清缓存 / 换设备不继承（当前不考虑账号体系）；
  - **关键边界**：移入垃圾桶的是「这条标题的展示」，后端下次抓到同一条仍应能在主列表重新出现（除非仍标记为已读）——需明确「按标题内容去重」还是「按当次抓取位置」去重，待拍板。
- **状态**：🔵 方向已提出，方案待设计

### TODO-6 入场动画（展示定位「将心注入，将爱注入」）

- **归属伏笔**：新增（用户 2026-08-19 提出）
- **现状**：当前站点**无任何入场动画**，打开即直接呈现三张热榜卡，缺少一次「与用户打招呼」的温柔开场；定位「将心注入、将爱注入」目前只写在 PRD 里，用户首屏**看不到**这句话。
- **待办内容**：新增**入场动画**——在用户进入站点时播放一段轻盈的开场，动画过程中**明确呈现网站定位文案「将心注入，将爱注入」**，让访客第一眼就读到这个「魂」，呼应 PRD 理念，把温度前置到第一秒。
- **候选方向（待拍板）**：
  - **触发时机**：首次访问播放一次（localStorage 记忆， revisited 不再重复）？还是「每次进入 / 每次刷新」都播？或仅在首次加载（页面冷启动）播、SPA 内路由切换不播？
  - **形式**：整页暖色蒙层（overlay）覆盖，居中浮现金色 / 暖玫瑰「❤」或「双心」轻轻搏动 → 文案「将心注入，将爱注入」逐字/整体淡入 → 蒙层柔化淡出，露出三卡内容；
  - **调性**：必须**轻柔、缓慢、无强闪**（守 P4 克制 + PRD「动效轻缓」），像一次深呼吸，而非炫技；时长建议 1.2~2s，可点击跳过；
  - **爱心 / 将爱注入的具象**：可用一个克制的「心跳 / 双心相拥」微动效点题「爱」，不喧哗。
- **约束 / 注意**：
  - **守 P5 包容（无障碍）**：必须尊重 `prefers-reduced-motion`，这类用户直接跳过动画、静态显字，**绝不强闪**；
  - **守 P4 克制**：不做冗长、不可关闭、遮挡过久的开场，避免变成「焦虑弹窗」式打扰；
  - **主题联动**：蒙层与文字配色须随 TODO-4 的主题（暖黄 / 浅绿 / 深黑）走，不可在深色主题下失联或刺眼；
  - **严格不回退无关**：纯前端展示层动画，不动后端、不碰真实数据契约；
  - 改动只限前端（新增一个 Splash / EntryOverlay 组件 + CSS 动画 + 轻量 TS 触发逻辑），后端无需动。
- **✅ 已落地（2026-08-19，讨论模式敲定后实现）**：
  - **设计拍板**：用户选「每日仪式」定位（B）+ 1 小时间隔策略（距上次打开 > 1h 才展示，localStorage `mini-hot-hub:last-entry`）+ 开场带当前时间桶金句小字（复用 `getQuoteAt`）+ 「进入」按钮带爱心图标。
  - **实现**：新增 `client/src/components/EntryOverlay.tsx` / `EntryOverlay.css`（暖色蒙层 → 暖玫瑰心轻轻呼吸 → motto「将心注入·将爱注入」缓淡入 → 金句小字 → 爱心+进入按钮；点击/ESC 后 650ms 柔化淡出）；`Home.tsx` 挂载（`useState(shouldShowEntry)` + Fragment 渲染）。
  - **无障碍**：`prefers-reduced-motion` → 动画禁用、静态显字显按钮（精神保留）。
  - **验证**：`tsc --noEmit` 0 错；`vite build` 通过（JS `index-s0A-4vbD.js` / CSS `index-BOUlxFl8.css`，29 modules）；grep 产物确认 `entry-overlay`/`last-entry`/motto×2/`entry-breathe`/`进入` 均在。
  - **已上线（测试模式，23:16）**：为方便测试，用户要求临时把 `shouldShowEntry()` 改为 `return true`（每次打开都播开场，原 1H 间隔逻辑整段注释在函数内）；部署后线上 JS=`index-CmCb7H2P.js`（`last-entry` 0 次 = 1H 逻辑已随注释消失）。**⚠️ 正式使用前必须恢复 1H 间隔逻辑并重新部署**（详见 MEMORY.md「临时禁用项」）。
- **状态**：✅ 已确定 / 已完成（已上线，当前为测试模式：每次打开都播开场）

### TODO-7 修正「未连接后端」错误态文案（去掉路由路径、更友好）

- **归属伏笔**：新增（用户 2026-08-19 提出）
- **现状**：当前未连接后端时，`HotCard` 错误态显示的文案为（见 `client/src/api/hot.ts` 中 `fetchHotPlatform` 的 catch 分支）：
  > `无法连接后端（/api/hot/zhihu），请确认服务已启动`
  两个问题：
  1. **暴露开发者术语**：`/api/hot/zhihu` 是后端路由路径，普通访客看不懂，与 PRD「P2 人性」相悖；
  2. **把排查责任推给用户**：「请确认服务已启动」暗示访客能启动后端——但访客无法控制后端启停，且真实成因常是**网络 / 地区限制**（如国内访问海外服务被 RST），并非"服务没起"。
- **待办内容**：将这段错误态文案重写为**温暖、诚实、对访客友好**的版本——去掉路由路径、不假设备后端由访客控制、可提示「稍后重试 / 检查网络」。
- **候选措辞（待拍板）**：
  - 「暂时连不上热榜服务，请稍后重试一下～」
  - 「热点数据加载失败了，可能是网络波动，过会儿再来看看 🍵」
  - 「暂时取不到热榜，先喝口水，稍后再来 🌿」
- **约束 / 注意**：
  - 守 PRD「P1 温暖 / P2 人性 / P3 诚实」——可温柔，但**不可掩盖真实状态**（仍要明确是"没拿到数据"，而非假装成功）；
  - 只改 `hot.ts` 中该字面量（`${route}` 不再进文案，或仅开发环境保留调试信息），错误态 `error:true` 呈现机制不变；
  - 不动后端、不碰真实数据契约。
- **状态**：🔵 方向已提出，方案待设计（具体措辞待拍板）

### TODO-8 用户投喂金句（提交 + 日后在旋转 banner 看到自己写的那句）

- **归属伏笔**：新增（用户 2026-08-19 提出，当前为「想法阶段」，先入 TODO 不实装）
- **现状**：旋转金句库已落地（`client/src/data/dailyQuotes.ts`，51 条内置，每 10 分钟按时间桶轮换）。但库是**写死在代码里的静态数组**，用户无法参与。
- **想法**：用户希望能有一个「提交金句」的入口——平时刷新闻 / 看到喜欢的句子，复制粘贴提交进去；日后某次打卡此网站时，旋转 banner 里能**偶遇自己当初写进去的那句**，形成「将心注入」的私人回响。
- **待办内容（待设计）**：
  - 一个克制的提交入口（如在页脚放一个「+ 投喂金句」小按钮，点开一个极简输入框 + 提交）；
  - 用户提交的句子进入「用户金句池」，并与内置 51 条一起参与旋转 banner 的轮换；
  - 用户日后访问时，能「撞见」自己写过的句子（确定性或加权出现，待拍板）。
- **候选方向（待拍板）**：
  - **存储**：当前是「方案 B 纯前端直连、无后端」。若仅存 `localStorage` → **只在本机本浏览器可见**，换设备 / 清缓存即丢失，不满足「某天在网站看到自己那句」的跨设备诉求；若要真正「随时打卡都能看到」，需引入后端持久化（或导出/导入 JSON、或同步到账号）。
  - **轮换方式**：用户金句与内置库合并后统一洗牌？还是单独一个小池子、以更高权重穿插？避免「提交了却几乎轮不到」或「刷屏」两个极端。
  - **内容边界**：用户自带内容、风险低；但若未来做成「跨用户共享池」，需加审核 / 举报 / 长度与敏感词约束（守 P5 包容，不出现攻击/歧视内容）。
  - **入口调性**：按钮 / 弹层须克制、不喧哗，契合整站温柔风（可参考 TODO-6 的入场动画调性）。
- **约束 / 注意**：
  - 纯前端展示层功能，不动后端真实数据契约、不违背「严格不回退」；
  - **关键边界（务必先拍板）**：「跨设备可见」必须后端支撑，当前架构做不到——落地前要先决定「仅本机」还是「上后端」。
  - 改动范围：前端新增一个 SubmitQuote 组件 + 本地存储读写（若上后端则另加 API）。
- **状态**：🔵 方向已提出，方案待设计

### TODO-9 名次徽章后续可能继续优化（方向性 / 待定）

- **归属伏笔**：新增（用户 2026-08-20 提出，当前仅「想法阶段」，先入 TODO 不实装）
- **现状**：当前徽章已迭代定稿为「简洁版」并上线——1/2/3 名共用同一颗鲜亮奶黄圆牌（光泽 3D 手法，仅数字区分），尺寸 24px；4~10 纯数字无圆牌；桌面整行悬停时前三名 `transform: scale(1.14)` 轻柔涨大（0.4s easeOutCubic，含 `prefers-reduced-motion` 降级）。配色抽成 `:root` 单组 `--rank-bg/--rank-fg/--rank-spot`，预留 TODO-4 三主题切换接口。
- **待办内容**：用户表示后续可能还会对徽章做进一步优化（具体方向未定，待届时讨论）；当前版本为阶段性定稿，不阻塞其他工作。
- **状态**：🔵 方向已提出，方案待设计（具体优化点待定）

### TODO-10 微信内置浏览器「从文章返回时整页重载」无法在网页侧消除（平台限制·记录不追）

- **归属伏笔**：新增（用户 2026-08-20 实测发现）
- **现状**：用户在三种容器里测过返回行为：
  1. **微信聊天里点网址进入** → 从标题进文章、再返回时，**整页会重载一次（回到顶部前先刷新）**；
  2. **WorkBuddy 内嵌浏览器进入** → 返回直接退回聊天（平台导航模型，网页改不了）；
  3. **手机自带浏览器进入** → 一切正常（有滚动记忆，见下）。
- **根因（已确认）**：①、② 都是**容器平台行为**，不是本站 bug——微信内置浏览器在「从外部链接返回」时会回收/重载后台网页；WorkBuddy 内嵌浏览器把打开链接当作「压栈一层」，返回即退出整个内嵌浏览器。这两件事**网页脚本都无法控制**。
- **已做的缓解（已上线）**：本站已加「滚动位置记忆」——离开 / 切后台时存位置（`sessionStorage` + 带 10 分钟过期的 `localStorage` 双保险），重新加载后自动滚回原处。所以在**自带浏览器**里返回能停在原位；在**微信**里即使重载，位置也能恢复（只是仍会有一次重载闪烁）。
- **为什么不追**：
  - 重载本身是微信平台机制，纯前端代码**没有干净的 API 能阻止微信重载页面**；
  - 曾考虑的 `window.open()` / `target="_blank"` 保活原页——在微信里外部文章链接会被其自身的浏览层接管，返回仍重载，不可靠，收益不抵复杂度；
  - 故按用户 2026-08-20 指示「不死磕」，仅记录为平台限制，不当作待修 bug。
- **给用户的可行建议（非网站改动）**：想要「返回留在热榜、无重载」的完整体验，在微信里**长按网址 → 选「在浏览器打开」**，或复制到手机自带浏览器——即退化到情况③，一切正常。
- **状态**：🔵 平台限制·已记录·不追（缓解=滚动记忆已上线；彻底消除需换容器，非代码能解）

---

## 二、已确认的用户偏好（参考，非待办）

- 桌面三卡**强制等宽**：`repeat(3, minmax(0, 1fr))`
- 响应式**保持现状**：<720px 竖堆
- 错误态卡片只需**同宽**即可（内容稀疏暂不在意）
- 手机端标题**自动换行**（今日头条式）—— ✅ 2026-08-18 已确认为正式方案

---

## 三、本会话已处理（已关闭）

- **手机端换行「临时方案」** → 2026-08-18 拍板去掉「临时」标注，确认为正式方案，移出待办。
- **删除遗留 Mock 数组** → 2026-08-18 已执行：`server/index.js` 删除 `WEIBO_HOT` / `ZHIHU_HOT` / `BILIBILI_HOT`，`PLATFORMS` 去 `items` 字段，删除 `buildPlatform()`，未登记平台改走 error 态；冒烟测试通过。
- **TODO-1 桌面标题「看出内容」增强 / TODO-2 卡片尺寸·字体偏小** → 2026-08-19 用户决定从开放待办中**去掉**（TODO-1 的核心痛点已由桌面奶黄气泡变相解决；TODO-2 用户选择不再单独跟进）。已从「一、待办项」移除，仅在此留痕。

---

## 四、整体推进方式

用户计划后续做一轮「问题汇总、逐个解决」，将上方待办逐项落地。新增伏笔随时追加到本清单与 `MEMORY.md`。
