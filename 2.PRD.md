# 2.PRD

# Vibe Coding 热点网页 · PRD

> 文档系列：`1.RESEARCH.md` → `2.PRD.md`（本文件）→ `3.TECH_DESIGN.md` → `4.AGENTS.md`
> 最后更新：2026-08-18 · 整合「产品理念（设计灵魂）」章节，并补充当前实现现状。

---

## 产品理念（设计灵魂）★ 2026-08-18 新增

本项目的核心是：**「将心注入、将爱注入」** —— 即使只是收集热点的小站，也希望每一个交互细节都透出温度，让人感到被认真对待、被温柔以待，而非面对一台冷冰冰的抓取机器。

具体落地为三层「尊重」：

1. **对内容尊重 = 真实不糊弄**：热搜数据必须真实抓取，失败如实告知，绝不用本地 Mock「假装成功」（即「严格不回退」原则，见下文技术约束）。
2. **对人尊重 = 温柔不冰冷**：出错时给「像人说话」的提示 + 明确的重试出路，而非裸红框；等待时给安抚而非空白转圈。
3. **对时间尊重 = 克制不骚扰**：不弹窗、不制造信息焦虑；热榜本就约 10 分钟一更，把节奏主动权还给用户。

**设计原则（冲突时按序号优先）**，是所有 UI / 配色 / 文案决策的终审依据：

- **P1 温暖优先于效率**：宁可贵一点、柔一点，也不要冷硬高效的科技感。
- **P2 人性化优先于机械感**：文案像人说话、错误态有温度、加载态有安抚、按钮给反馈。
- **P3 诚实优先于漂亮**：严格不回退，绝不为「好看」造假数据（最高优先级技术约束）。
- **P4 克制优先于堆砌**：少即是暖，不过度装饰、不信息过载。
- **P5 包容优先于炫技**：桌面 / 手机都好用、正文字号看得清、动效轻缓不引发不适（尊重「减少动效」偏好）。

**视觉气质指引（硬约束）**：温暖、柔软、低饱和、有呼吸感；拒绝冷硬科技风、高饱和刺眼色、拥挤排版。圆角柔和、柔影淡、正文须清晰可读、动效轻缓无强闪。候选暖色主调（全站暖奶黄 / 奶黄+紫 / 奶油白+焦糖金）在优化阶段拍板，但**均须满足此温暖基调**。本气质与早期设定的「罗小黑战记清新治愈风」一脉相承、互为强化。

**如何判断「做到位了」（验收视角）**：一个第一次来的用户，是否觉得「这个网站有点不一样，挺舒服」；出错时是否不慌、有路可走；每次打开是否像被温柔接待。

---

## 产品概述

一个多平台热点聚合网页，一个页面即可浏览微博、知乎、B 站等平台当下热度最高的事件，省去自己到处找热度的时间。视觉走「清新、治愈、罗小黑战记」风格，并把最喜欢的角色写进页面。访客打开链接即可浏览，无需登录。

> 在「产品理念」章节之上，本项目的更高追求是**温度**：即便只是把热搜摆出来，也要让人感到被认真对待、被温柔以待（详见上方「产品理念（设计灵魂）」）。

## 目标用户

* 想快速扫一眼各平台热点的上班族、学生
* 对 Vibe Coding / 全栈开发感兴趣的学习者（本项目即作为 12 天打卡练「AI 掌控力」的载体）

## 核心功能

### MVP（必须做）

1. **首页（极简卡片分区 + 文字列表）**
   * 三个平台各一个极简卡片分区：微博热搜、知乎热榜、B 站热搜
   * 每张卡片：平台名、榜单名、Top 10～20 列表
   * 每条：排名、标题、跳转链接（可选热度）
   * 顶部平台 Tab 筛选（全部 / 微博 / 知乎 / B 站），非独立页
   * 形态取舍：保留「卡片网格」外壳，但卡片内用纯文字列表（不堆大图），融合极简思路
2. **全局标题栏**
   * 左：站名 / logo；右：刷新按钮 + 设置入口；所有页面共用
3. **设置**
   * 「显示哪些平台」多选开关
   * 刷新方式（手动 / 自动）
4. **数据源（轻量方案）**
   * ~~MVP 纯前端直连现成聚合 API（如 vvhan / 60s API）或静态 Mock，不引入后端~~
   * ⚠️ **实现现状（2026-08-18）**：项目已落地**后端 Node.js + Express**（`server/`），经后端 `fetch` 真实第三方聚合接口（uapis.cn）取数，**不再采用纯前端直连**，且**已删除静态 Mock、改为硬性「严格不回退」**（抓取失败即如实返回错误态 + 重试，绝不回退假数据）。早期设想中「或静态 Mock」已被明确否决。
5. **异常处理**
   * 单平台数据失败不影响其他卡片（✅ 已实现：单卡错误态隔离）
   * 失败卡片显示友好提示（✅ 已实现：红框 + 像人说话的文案 + 重试按钮）

> 本期不做：搜索、登录 / 账号系统、设置深度内容（壁纸 / 主题 / 动画等视觉定制）

### 后续可以做

* 自建后端 + 缓存：`GET /api/hot/:source`（source = weibo | zhihu | bilibili），可选 `GET /api/hot` 一次返回全部；每平台独立缓存 TTL 5～10 分钟，展示「更新于 × 分钟前」
  * ⚠️ **实现现状**：后端与独立缓存（TTL 默认 600s，可 `CACHE_TTL` 调）**已落地**，页脚动态显示「更新频率约 N 分钟」。
* 更多平台（抖音、百度等）
* 分类 Tab（科技 / 娱乐）— 含候选「实习 / 招聘 / 校招」方向（见 `TODO.md`）
* 站内搜索、历史榜、暗色模式
* 设置深度内容（壁纸 / 主题 / 动画）：解冻本期不做项
* 公网 HTTPS 部署（GitHub 托管代码）— 含「手机端改网址登录替代局域网」方向（见 `TODO.md`）

## 界面设计要求

* 融合形态：参考今日头条的「信息清晰、分区明确」+ 极简热榜 / AnyKnew 的「留白舒适、文字为主」
* 视觉基调：极简中性色打底 + 清新治愈罗小黑风，最喜欢的角色自然融入（MVP 即体现，把握分寸不显幼稚）
* 响应式：桌面多列（三卡片并排 / 网格）、手机单列占满，无横向滚动
* 页脚：学习项目说明、数据来源、非商用声明（✅ 已实现：4 行页脚含更新频率联动与联系邮箱）
* **温度要求（2026-08-18 新增，对应「产品理念」P1/P2）**：在以上视觉基调之外，所有界面须透出温暖——配色偏暖、文案像人说话、错误与加载态有安抚感，详见「产品理念（设计灵魂）」。

## 技术栈

* 前端：React + TypeScript + Vite + CSS（✅ 已实现）
* 后端：MVP 原计划不引入；**实现现状：已采用 Node.js + Express（`server/`，端口 3001）**（✅ 已落地）
* 部署：MVP 可本地 / 静态托管；后续可选 Vercel + Railway（不强制统一）
