# 3.TECH\_DESIGN

> 状态：技术选型已定（MVP 纯前端）
> 视角：MVP 纯前端，不引入后端

## 架构总览

* 定位：MVP 为**纯前端**实现，不引入后端服务
* 数据流：浏览器 → 免费聚合 API（已解决 CORS）/ 静态 Mock → 解析为统一类型 → 渲染
* 分层：UI 层 → 数据适配层 → 类型层 → 配置层 → Mock 层
* 演进：后续可加 Node + Express 后端（routes / services / cache），本期不做

```
[浏览器]
   │  fetch
   ▼
聚合 API / Mock（CORS 已解决）
   │  解析
   ▼
HotPlatform[]  ──►  渲染 HotCard（顶部 Tab 筛选）
```

## 技术栈

* 前端框架：React + TypeScript + Vite
* 样式：纯 CSS + CSS 变量（按组件拆分文件，变量统一管理治愈风配色）
* 后端：MVP 不引入（后续 Node.js + Express）
* 数据源：vvhan 类免费聚合 API（开发期先用本地 Mock JSON，最后接真实 API）
* 前端缓存：内存 Map + TTL 时间戳
* 角色 / 视觉：现成角色图 / SVG 素材 + CSS 营造氛围（治愈风）
* 部署：MVP 本地 `npm run dev`；后续 Vercel / Netlify 静态托管

## 项目结构

vibe-hot/

└── src/

├── components/   # HotCard、Layout、TabBar、Settings

├── api/          # fetchHot（聚合 API / Mock 封装）

├── types/        # HotItem、HotPlatform

├── mock/         # 本地 Mock JSON（结构严格照 HotPlatform）

├── config/       # 显示平台、刷新方式等设置

├── styles/       # 全局 CSS 变量（治愈风配色 / 圆角）

└── App.tsx

（后续 server/ 目录预留，本期不建）

## 数据模型

### HotItem

* rank: number
* title: string
* url: string
* heat?: string

### HotPlatform（接口响应统一契约）

* source: string      # weibo | zhihu | bilibili
* sourceName: string  # 微博
* listName: string    # 热搜榜
* updatedAt: string   # ISO 8601
* items: HotItem\[]
* error?: boolean
* message?: string

## 核心流程（MVP）

1. 用户打开首页 → 前端按「显示平台」设置请求各平台数据
2. 前端调用 fetchHot → 开发期读本地 Mock JSON，联调后切真实聚合 API → 解析为 HotPlatform
3. 按平台渲染 HotCard（顶部 Tab 筛选全部 / 单平台）
4. 某平台失败 → 该卡片 error 态，其他平台正常展示

## 开发环境配置

* MVP 纯前端：无 `/api` 代理；直接 fetch 聚合 API（或读本地 Mock）
* 环境变量：`VITE_API_BASE` 指向聚合 API 域名（Mock 模式下可空）
* 后续后端接入时才有 Vite proxy → localhost:3001 / Nginx 反代

## 数据方案备注

* 主路线：vvhan 类免费聚合 API（CORS 已解决，浏览器直连）
* 救急：本地 Mock JSON（开发期 / API 不稳定时）
* 不推荐：直接抓官方接口（CORS 限制）、HTML 爬虫
* 具体 API 域名 / 路径在动手前实测连通性，不凭记忆写死
* 背景：本项目为 21 天打卡练「AI 掌控力」的载体，MVP 以快出活、早见页面为目标

## 技术选型说明（含取舍理由）

1. **数据源 → vvhan 类聚合 API + 本地 Mock**：免费、已解决 CORS、纯前端可取真实数据；自抓官方接口需后端、纯 Mock 无真热度、60s-api 平台覆盖略逊。
2. **样式 → 纯 CSS + 变量**：治愈风需精细自定义配色 / 圆角，最自由；Tailwind 类名乱、UI 组件库太重不搭。
3. **Mock → 本地 JSON 文件**：直观对应数据模型，改起来不需懂逻辑；内联假数据混在代码乱、Mock 服务多依赖。
4. **缓存 → 内存 Map + TTL**：MVP 只需避免重复请求，几行代码；localStorage 需序列化、Service Worker 过度。
5. **角色视觉 → 素材 + CSS 氛围**：最快出治愈感，不陷进「代码画画」；备选纯 CSS 手绘（见下）。
6. **部署 → 本地 dev + Vercel / Netlify**：静态站零运维；自建服务器 / Nginx 重、Railway 是给后端用的。

## 角色落地备注

* MVP：现成角色图 / SVG 素材 + CSS 氛围（柔和配色、圆角卡片、舒服字体、角色点缀）
* 备选：若治愈风效果不理想，改用「纯 CSS 手绘角色」
* 版权：非商用学习项目，使用同人 / 自制简笔治愈风素材规避风险
