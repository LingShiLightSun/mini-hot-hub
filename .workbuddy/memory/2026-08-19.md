# 2026-08-19 工作日志

## 平台选择行（TabBar）悬停/选中反馈补齐
- **背景**：用户指出「列表条目有悬停反馈，但微博/知乎/B站选择行没有」，批评不够"用心"（呼应 PRD 将心注入定位）。
- **诊断**：TabBar 原有 hover 仅改字色（几乎不可见）、选中态用极淡 accent-soft 与列表暖语言脱节；本质是之前只给列表条目加了反馈、漏了顶部标签栏。
- **改动**（`client/src/components/TabBar.css`，纯 CSS）：
  - hover（仅 hover 设备）：背景 `#FCF4E8`（与 HotCard 列表悬停**同一暖奶油底**），字色加深到 `--text-h`，全页温度连贯；
  - is-active：背景加深一档 `#F3E4CC` + 摩卡金 `--accent` 字 + 600 + 柔和暖影 `rgba(176,125,59,.18)`，像"坐定被托起"；
  - `:active` 极轻 `translateY(1px)` 给点击回应。
- 用 `@media (hover:hover)` 包裹，手机端无 hover 不误触。未动任何 TS/后端。
- HC 未单独跑编译（仅 CSS 改动，前序 tsc 已通过）；用户在跑的 Vite 实例 HMR 即可见。

## 标签悬停可见性二次打磨
- **用户反馈**：选中态明显，但悬停态仍不显眼。
- **根因诊断（关键）**：标签栏直接坐在**页面奶油底 #FBF6EC** 上；首版悬停用了列表白卡上的 #FCF4E8，奶油叠奶油≈无反馈。不像列表（白卡上才看得见）。
- **修法**（非无脑加暗）：悬停底改为**明显深于页面底**的 #F5EAD9 + 极轻暖影(0 1px 4px rgba(176,125,59,.10)) 让其「浮起」；选中态也顺势加深到 #EFE0C8，使 hover / active 层级分明。
- **经验**：同一颜色在不同表面（白卡 vs 奶油底）可见性天差地别，调悬停底色必须对照它所在的实际背景，而非照搬别处的同名牌。

## 桌面奶黄气泡（tooltip）不显示的 bug 修复
- **用户反馈**：悬停被截断的标题，气泡根本不出现。
- **根因（结构性裁切）**：原实现把 `text-overflow: ellipsis` 所需的 `overflow: hidden` 直接加在 `<a class="hot-card__title-link">` 上；而气泡是 `<a>` 的子元素、定位在标题**上方**（`bottom: calc(100% + 8px)`，在 `<a>` 盒子之外）。`overflow:hidden` 会裁掉所有超出盒子的绝对定位后代 → 气泡被无声切掉，与"是否截断"无关。
- **修法**：省略/换行从 `<a>` 下移到**内层 `<span class="hot-card__title-text">`**；`<a>` 只做 `position:relative` 锚点、不再设 overflow。`useIsTruncated` 的 ref 也移到内层 span（测真正被截断的元素）。桌面媒体查询的省略规则同步改到 `.hot-card__title-text`。
- **验证**：`tsc --noEmit` 通过（TSC_EXIT=0）。用户在跑的 Vite HMR 即可见；需桌面≥720px + 标题真正被截断（如 B站 长标题）才触发，短标题走原生 title 兜底（设计如此）。
- **遗留（用户说"后面再商量"）**：气泡仍定位在标题**上方**，第①名那条会压到卡片表头；若用户实测后觉得碍眼，可改到下方或右对齐。

## 桌面奶黄气泡位置定稿（方案② 右上溢出）
- **用户反馈**：气泡在标题正上方会完全挡住自己的表头（"微博·热搜"），带来不适感，不符合定位。明确表态"不挡自己的锚点，要挡就挡其他列"。
- **决策**：经三方案对比（①上方挡表头 / ②右上溢出挡其他列 / ③下方挡下一行），用户拍板**方案②**。
- **首次改动（不到位）**：气泡定位 `left:0` → `right:-10px`，小三角翻到右下。
- **用户实测反馈**：气泡根本没盖住其他列，只是三角从左变到右——根因是 10px 溢出被 grid gap（~20px）完全吸掉，气泡整体仍留在卡片内部。
- **二次修正确认到位**（`client/src/components/HotCard.css`，纯 CSS）：
  - `right:-10px` → `right:-180px`（大幅右溢出 180px，跨过列间距、清晰盖到右侧其他列）
  - 小三角翻回 `left:16px`（指回标题文字，保住气泡↔标题的视觉连接）
  - 依赖前提：`.hot-card` 无 `overflow:hidden`、气泡 `z-index:20`（正数层级，会绘制于邻居列 z-index:auto 之上）
- **边界提醒**：最右一列（B站）无右侧邻居，气泡会探出视口右缘（左半可读，可接受）；若日后觉得盖邻居也不适，可退回方案③（下方）。
- 移动端（<720px）气泡仍 `display:none`（标题自动换行不溢出，避免误触发）。
- **教训**：实现"溢出盖邻居"这类需求，溢出量必须明显大于布局间 gap，否则会被布局吞掉、看起来等于没做。

## 待续（用户侧）
- TODO：卡片尺寸/字体偏小、实习分类、手机改网址登录，均未动。
- **新增 TODO-5：桌面奶黄气泡位置可移动（拖拽 / 可调）**。气泡已按方案②（右上溢出 right:-180px）定稿到位，用户顺延提出后续增强：希望气泡位置可移动（拖拽或预设位切换，仅桌面≥720px），把体验交还给人，契合「将心注入」。已写入 `TODO.md` 作为 TODO-5（🔵 方向已提出，方案待设计）。约束：不破坏"溢出盖其他列、不挡自己表头"的既定基线；改动限 HotCard.css + 必要 TS 状态，后端无需动。

## 卡片表头优化（方案 D：去尾巴 + 品牌色图标）
- **用户诉求**：① 去掉冗余「热榜/热搜榜」尾巴（整页标题已是「今日热榜」，重复显臃肿）；② 去尾后只剩平台名太素，需补回温度。
- **决策**：讨论后用户拍板「先试 D，不行再退 C」。D = 去掉 listName 尾巴 + 表头加一个品牌色小图标（chip 28px 圆角 8px + 首字白字），每平台一抹自家色；C = 三平台统一摩卡金（无品牌色）。
- **改动**（`client/src/components/HotCard.tsx` + `HotCard.css`，纯前端，后端未动）：
  - 新增 `PLATFORM_META` map（按 sourceName 映射 `{initial, color}`）：微博 #E08A4E(暖橙)/知乎 #6E9BC4(柔蓝)/哔哩哔哩 #E78AA6(柔粉)；颜色**略加深**以保白字可读（守 P5 包容），未知平台回退首字+摩卡金 #B07D3B。
  - 表头 JSX：去掉 `{listName}` 拼接，改为 `<span class="hot-card__badge" style=color>{initial}</span> + <h3>{name}</h3>`；badge `aria-hidden`（装饰）。
  - CSS：`.hot-card__head` 改 `display:flex; align-items:center; gap:10px`；新增 `.hot-card__badge`（28×28、圆角8、白字13/600）。
- **退回 C 的路径（已预留）**：把 `PLATFORM_META` 三行 color 全改成 `#B07D3B` 即可，无需改结构。
- **遗留待定（用户未回答）**：表头显示名仍用后端 `sourceName`「哔哩哔哩」（4字），未改成更轻的「B站」；若三卡表头齐整度在意，可后续改展示别名。
- **验证**：`tsc --noEmit` 通过（TSC_EXIT=0）；用户在跑的 Vite 实例 HMR 即可见。

## 表头图标微调：知乎雾蓝 + 哔哩哔哩→B站
- **用户诉求**：① 知乎的柔蓝 `#6E9BC4` 太冷、与全站暖色系不搭，想要一个「与色系较搭配的蓝色」；② 把表头「哔哩哔哩」改成更轻的「B站」。
- **改动**（`client/src/components/HotCard.tsx`，纯前端、后端未动）：
  - 知乎 color `#6E9BC4` → **`#5E8098` 雾蓝**（去饱和、略加深）：比纯冷蓝更灰，与暖奶油底协调，且白字对比度约 4:1（守 P5 可读）。这是「搭配色系的蓝」而非品牌冷蓝。
  - 新增 `SOURCE_DISPLAY` map：`'哔哩哔哩' → 'B站'`，组件内 `rawName = data?.sourceName ?? sourceName`，`name = SOURCE_DISPLAY[rawName] ?? rawName`；meta 仍按 rawName 查色（兼容数据态传「哔哩哔哩」、加载/错误态传「B站」两种），故 `PLATFORM_META` 同时保留 `'哔哩哔哩'` 与 `'B站'` 两键，initial 均为 `'B'`。
  - 表头标题现显示「B站」、badge 首字「B」；三卡表头齐整度提升。
- **验证**：`tsc --noEmit` 通过（TSC_EXIT=0）；HMR 即可见。

## TODO 清单调整（移除 1/2，新增主题色 + 浏览垃圾桶）
- **移除 TODO-1（桌面标题看出内容增强）与 TODO-2（卡片尺寸/字体偏小）**：用户决定从开放待办去掉。TODO-1 核心痛点已由桌面奶黄气泡变相解决；TODO-2 用户选择不再单独跟进。已从 `TODO.md`「一、待办项」删除，仅在三、已关闭留痕。其余项重新顺号 1~5。
- **新增 TODO-4 主题色切换（暖黄 / 浅绿 / 深黑）**：预设三套**均为护眼色**；暖黄为默认、锚定 PRD 暖调，浅绿/深黑为用户自选护眼变体。机制：`data-theme` 切换 CSS 变量族 + 切换器入口 + localStorage 持久化；守 P5 可读；既已写死视觉（气泡/品牌 chip/悬停）须随主题联动。纯前端，后端不动。
- **新增 TODO-5 浏览垃圾桶（已读移入 + 浏览记录）**：点击打开的标题自动移入垃圾桶、不再占主列表；垃圾桶兼浏览记录（可回看/还原/清空）。仅前端 localStorage 过滤，不改后端、不违反严格不回退；关键边界=按标题内容还是当次抓取位置去重，待拍板。
- 同步精简 `MEMORY.md`（此前超限被截断，借此次合并去重，保留定位/约束/结构/调试铁律/数据语义/待办索引）。

## 新增 TODO-6 入场动画（展示定位「将心注入，将爱注入」）
- **用户诉求**：后续希望加入**入场动画**，且动画中**一定写上本网站定位「将心注入，将爱注入」**——让访客首屏第一眼就读到这个「魂」。
- **写入 `TODO.md` 作为 TODO-6**（🔵 方向已提出，方案待设计）。要点：
  - 现状：站点无任何入场动画，打开即直接呈现三卡，定位文案只在 PRD 里、首屏看不到。
  - 触发时机待拍板（首次访问 localStorage 记忆 / 每次进入 / 仅冷启动）；形式：整页暖色蒙层 → 居中双心轻搏 → 文案淡入 → 蒙层柔化淡出露三卡；调性必须轻柔无强闪（守 P4 + PRD 动效轻缓），时长 1.2~2s 可跳过。
  - 约束：守 P5 尊重 `prefers-reduced-motion`（该类用户直接静态显字、绝不强闪）；守 P4 不可遮挡过久/不可关闭变打扰；蒙层配色须随 TODO-4 主题（暖黄/浅绿/深黑）联动；纯前端新增 EntryOverlay 组件 + CSS 动画 + 轻量 TS，后端不动。
- 当前开放待办已扩至 1~6（1实习分类 / 2网址登录 / 3气泡可移动 / 4主题色 / 5浏览垃圾桶 / 6入场动画）。

## 根目录 package.json 新建（聚合 scripts）
- **事实校正**：之前误记"根目录本有 package.json"——实际不存在。本次新建 `/package.json`（name mini-hot-hub, private）。
- **用户拍板**：加聚合脚本，方便本地一行起前后端（用户原话"应该做：根加 dev:client / dev:server"）。
- **scripts**：`dev:client` = `npm --prefix client run dev`；`dev:server` = `npm --prefix server run dev`；顺带加 `start:server` = `npm --prefix server run start`、`build:client` = `npm --prefix client run build`（贴合刚做的 dist 工作流）。
- **未引入 concurrently** 等依赖（避免过度工程）；`npm run` 已确认 4 个 script 均解析成功。
- **用法**：根目录开两个终端分别 `npm run dev:server`（后端3001，带 --watch 自动重启）与 `npm run dev:client`（前端5173）；或部署时 `npm run build:client` 出 dist/。
