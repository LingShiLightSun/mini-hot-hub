# 2026-08-20 工作日志

## 安装 uitest-main 技能（08:35）
- 用户桌面 `uitest-main.zip`（156KB）实为**57 种 UI 风格静态展示页**（index.html 画廊 + 48 个 styles/*.html 模板），**非标准 WorkBuddy 技能格式（无 SKILL.md、无脚本）**。
- 安全审计：纯静态 HTML/CSS，无可执行脚本/package.json，仅 Tailwind+Google Fonts CDN，**P2 安全**。
- 处理：补写合格 `SKILL.md` 外壳，包装为真正技能，安装到用户级 `~/.workbuddy/skills/uitest-main/`（index.html + README.md + styles/ + SKILL.md）。
- 用途：做 UI 设计时按风格名拉对应 `styles/NN-xxx.html` 作视觉参考（已在 SKILL.md 写速查表）。
- 临时解压目录 `/tmp/uitest-audit` 已清理。

## 名次徽章改用 Claymorphism（09）视觉语法（08:43 起）
- 用户从 uitest-main 库里**选定 Claymorphism（09）**作为徽章视觉语法（对比 Minimalism 01 后拍板）。
- 落地 `client/src/components/HotCard.css`：把 1~3 名徽章从原暖金改造成**粘土风三主题色圆牌**——
  - 1 = 奶黄(#fbe7a6→#efc75e) = 奶糖
  - 2 = 绿(#bfe9b6→#8fcf88) = 叶子
  - 3 = 黑/月(#8c8ca6→#4a4a63) = 月亮
  - 4~10 = 中性奶白粘土圆牌（安静同族）
- Clay 公式：渐变底(145deg) + 白边(1.5px) + 三层影(外层彩色硬影托起 + 右下暗影体积 + 左上高光光泽)，从 `09-claymorphism.html` 提取并缩放到 24~26px 小圆牌。
- 构建：`vite build`（国内镜像 + mv 绕过 safe-delete）成功；`vite preview --port 4173` 已起，**本地预览 http://localhost:4173/**。
- 状态：**未部署**（等用户「重新部署」指令）。徽章配色为 TODO-4 三主题色选择功能的默认态，后续可加切换。

## 徽章风格从 Claymorphism 切到「暖色光泽 3D」（#05 改编）（08:52）
- 用户看完 Clay 预览后，要求换成 uitest-main 画廊 **#5 = 3D & Hyperrealism** 风格。
- 判断：原版 #5 是深色 slate 底 + 铬金/金属渐变 + 旋转 3D，与本站奶油治愈风冲突，且其自身规格标 Performance:Poor / A11y:Limited / Mobile:Low；24px 小圆牌上旋转 3D 在手机端无意义。
- 做法：**取 #5「立体光泽」之魂、换暖色之皮**——顶部径向高光 + 饱和体渐变(160°) + 柔和写实投影(0 6px 12px -3px tinted)，无白边、无旋转。1=奶黄糖衣/2=绿宝石/3=黑月光石，4~10 暖白光泽圆牌。
- 已 rebuild + `vite preview :4173` 刷新预览。相对 Clay 是「哑光软糯 → 透亮光泽」的差别。
- 待用户定：保留此光泽 3D / 回退 Clay / 或真做深色铬金原版（不推荐）。仍未部署。

## 重要概念纠正：三种颜色 = 主题模式，不是名次固定色（09:0x）
- 用户澄清：「奶黄 / 绿叶 / 黑夜」是三种**可切换的颜色模式**，当前只激活默认「奶黄」模式；之前把 rank1=奶黄/rank2=绿/rank3=黑 写死是概念错位（绿黑会跑出到奶黄模式里，自相矛盾）。
- 修正 `HotCard.css`：徽章改为**主题驱动**——默认奶黄模式下 1/2/3 全是奶黄同族仅按深浅区分（1深最饱和 / 2中 / 3浅），4~10 中性暖白（跨主题不变）。
- 配色抽成 `:root` 的 `--rank1-bg/--rank2-bg/--rank3-bg/--rank-fg/--rank-spot*` 变量，注释标明：后续做切换只需在根节点加 `.theme-green`/`.theme-night` 类覆盖这组变量（组件结构不动）。**这即是 TODO-4 三色模式切换的种子实现**。
- 仍保留 #05 光泽 3D 手法（顶部径向高光+饱和体渐变+柔和写实投影），只改了颜色逻辑。rebuild 成功，预览 :4173 已刷新。绿(#6fb85f)/黑(#34344a) 名次色已从产物移除。
- 仍未部署；开场动画 1H 逻辑仍测试模式。

## 徽章层级细化：2/3 名不再"变淡发脏"（09:05）
- 用户反馈：1 名 OK，但 2/3 名用"往米白褪"做层级会发灰发"脏"（像脏芥末色）。
- 修正：1/2/3 名**全部保持同样鲜亮高饱和奶黄**，名次改用**尺寸+数字**区分——1=27px(最大+暖金柔光)、2=25px、3=23px。避免低饱和褪色。rebuild 成功，预览 :4173 已刷新。
- 绿/黑主题变量块仍不写（用户说暂不用顺手加），TODO-4 时再补。

## 徽章进一步做减法：1/2/3 同款 + 4~10 纯数字（09:09）
- 用户要求更简洁、减视觉负担：1/2/3 名换成**同尺寸同亮度**的同一颗鲜亮奶黄圆牌（仅数字区分）；4~10 **去掉圆牌、回归纯数字**（暖灰字，同最早设计）。
- 层级现为：圆牌=前三名（被看见），纯数字=其余。变量精简为单组 `--rank-bg/--rank-fg/--rank-spot`，TODO-4 绿/黑切换只需覆盖 `--rank-bg`。CSS 体积 9.71→8.75kB。rebuild 成功，预览 :4173 已刷新。
- 仍未部署；开场动画 1H 逻辑仍测试模式。

## 徽章加 hover 微交互（灵动感）（09:15）
- 用户讨论后拍板：桌面「整行悬停」时，前三名(1/2/3)奶黄圆牌 `transform: scale(1.14)` 轻轻涨大（0.18s ease），配 `box-shadow` 过渡。用 transform 而非改尺寸，避免重排挤动标题。
- 已加 `@media (hover:hover)` 触发 + `@media (prefers-reduced-motion: reduce)` 关闭缩放（无障碍降级，与开场动画一致）。4~10 纯数字不动。
- rebuild 成功（CSS 9.18kB），预览 :4173 已刷新。仍未部署。
- 微调：0.18s 太急、用户嫌不舒服；改为 `0.4s cubic-bezier(.33,1,.68,1)`（easeOutCubic）缓缓涨起、轻轻落回。rebuild 成功（CSS 9.23kB）。
- 部署上线 + TODO-9：用户要求「部署上线」并把「后续可能优化徽章」记入 TODO（已加 TODO-9，🔵 待定）。CloudStudio 重新部署成功（同沙盒 191d2bb… 覆盖更新），线上链接 https://191d2bb193714d1cbb3b7aeb288f19f6.app.workbuddy.link，现为简化版徽章 + 0.4s 轻柔 hover。⚠️ 开场动画 1H 逻辑本次仍**未恢复**（测试模式：每次都播）——已提示用户，待其确认是否恢复后重新部署。
- 用户确认：开场动画 1H 逻辑保持测试模式（每次都播），本次不恢复、不重新部署。

## 确立「手机端优先」+ 手机标题优化讨论（10:46）
- 用户确立：**以后优化的重点对象都是手机端**（已写入 MEMORY.md 长期偏好）。所有新优化优先保证手机端体验。
- 手机端标题优化第一点：① 所有标题单行（当前手机端是自动换行、长短不一）；② 同时要有方法像电脑端一样"有气泡显示完整标题"（当前手机端气泡 `display:none` 且 hover 不触发）。
- 现状对齐：电脑端 `HotItem` 已有气泡 DOM（`.hot-card__bubble`），`useIsTruncated` 检测溢出才显示，hover/focus 触发；手机端 `<720px` 标题 `white-space:normal` 换行 + 气泡 `display:none`。即手机端靠"换行显示完整"避免截断。
- 待用户拍板交互模型（标题右侧小气泡钮 vs 点标题弹气泡 vs 2行折中）。讨论中。
- 用户选 A（标题右侧小气泡钮）。已实现：① 手机端标题统一单行省略（`.hot-card__title-text` 基础样式改为 nowrap+ellipsis，删原 `<720px` 换行 + 删 `@media(min-width:720px)` 单行规则，桌面手机统一一行）；② 溢出时标题右侧显示极小「⋯」钮，点击切换 `.is-open` 弹奶黄气泡看全文，点列外/Esc 关闭；③ 桌面 hover 气泡保留（改 `.hot-card__title-col:hover` 触发，包 `@media(hover:hover)`）；④ 气泡锚点从 `<a>` 移到 `.hot-card__title-col`，避免 `<a>` 内嵌 `<button>` 语义问题；⑤ 手机端气泡定位改 `right:0` + `max-width:min(280px,100vw-32px)` 防飞出屏幕。rebuild 成功（CSS 9.59kB），预览 :4173 刷新。未部署。
- **部署上线（10:56）**：用户只有手机看不了电脑预览，要求直接部署看真实效果。重新 build（确保 dist 最新）后 CloudStudio 部署成功（同沙盒 191d2bb… 覆盖更新），线上现为**手机端单列标题 + 溢出「⋯」气泡**版。开场动画 1H 逻辑仍测试模式（每次都播，用户此前确认保持现状）。
- **修复手机端横向滑动（11:00）**：用户报告手机端可左右滑（本应只有上下滑）。根因：① `.hot-card` 作为 grid item 默认 `min-width:auto` 易被内容撑宽导致列溢出；② 桌面气泡 `right:-180px` 带 180px 溢出隐患。修复：给 `.hot-card`/`.hot-card__item` 补 `min-width:0`（治本，允许收缩），并在 `@media(max-width:719.98px)` 给 `.home-grid` 加 `overflow-x:clip`（手机端禁横滑、不影响桌面气泡）。重新 build + CloudStudio 部署成功（同沙盒覆盖）。待用户手机验证。
- **气泡互斥（11:11）**：用户从「将心注入」角度提出——一个人同一时刻只应专注看一个标题，点 A 的⋯再点 B 的⋯时，A 的气泡应消失。将 HotItem 各自独立的 `bubbleOpen` state 提升为 HotCard 单一 `openRank` 受控状态，实现互斥：点新关旧、点同一 toggle、点卡片外任意处 / Esc 关闭。重新 build + CloudStudio 部署成功（同沙盒覆盖）。
- **去掉手机端点击蓝色选中框（11:19）**：用户报手机端每次点标题/⋯/按钮都出现蓝色选中高亮框，影响观感（原生 App 没有）。根因：移动浏览器默认的 `-webkit-tap-highlight-color` 点击高亮 + `:focus` outline。在 App.css 全局加 `*{ -webkit-tap-highlight-color:transparent; -webkit-touch-callout:none }` 关高亮+长按菜单，并 `* :focus:not(:focus-visible){outline:none}` 去掉触摸/鼠标点击的焦点框但保留键盘 Tab 焦点框（无障碍）。重新 build + CloudStudio 部署成功（同沙盒覆盖）。
- **滚动位置记忆（11:45）**：用户报从标题进入文章、退回后页面被打回顶部（不符合习惯）。根因：手机浏览器回收后台标签 → 回来时 SPA 重新加载、无滚动恢复 → 停顶部。修复：Home.tsx 关 `history.scrollRestoration='manual'`；离开/切后台（`visibilitychange` hidden + `pagehide`）存 `sessionStorage` 的 `scrollY`；数据就绪（`platforms.length>0`）后 `requestAnimationFrame` 内 `scrollTo(0,y)` 恢复并清除键。首次访问无记录则不跳。重新 build + CloudStudio 部署成功（同沙盒覆盖）。
- **滚动记忆加固（12:23）**：用户测三种入口——① 微信内置浏览器进：每次回顶端（微信激进回收+常清空 sessionStorage）；② WorkBuddy 内嵌浏览器进：返回直接退回 WorkBuddy 聊天（平台导航栈模型，网页层改不了）；③ 标准浏览器进：一切正常（证明修复有效），深色模式为浏览器自动反色、非本站做。加固：加 `localStorage` 双保险（带 10min 有效期 `SCROLL_MAX_AGE`），sessionStorage 丢时 fallback 到 localStorage；标准浏览器走原通道不受影响。重新 build + CloudStudio 部署成功（同沙盒覆盖）。WorkBuddy 内嵌的"返回退出到聊天"属平台行为，已建议用户用外部/系统浏览器打开链接。
- **微信「返回重载」记录不追（13:59）**：用户复测确认微信内置浏览器从文章返回时仍会整页重载一次（平台行为），网页侧无干净 API 可阻止；曾考虑的 `window.open()`/`target=_blank` 保活原页在微信不可靠。按用户指示"不死磕"，记入 TODO-10（平台限制·已记录·不追，缓解=滚动记忆已上线；彻底消除需换容器）。
- **主题切换功能落地（14:06 起）**：用户要求主题切换不单独罗列、收进「设置」。讨论拍板：右上角齿轮 + 设置面板（主题切换 + 预留位）+ 三小图标（暖阳/新芽/月亮，内联 SVG 无 emoji）。实现：① 全站配色写死 → `App.css` 变量族（`:root` 暖黄默认 + `:root[data-theme=green/dark]` 覆盖），名次圆牌/气泡/骨架/Tab交互态/入场渐变/错误爱色全部随主题联动，品牌 chip 保持原色；② 右上角 `.home-settings` 齿轮 + 新 `SettingsPanel.tsx/.css`（底部弹出卡 / 桌面居中）；③ `localStorage['mini-hot-hub:theme']` 持久化 + `index.html` 内联脚本防 FOUC；④ 设置「更多」区为 TODO-5/8 留结构（规划中）。移动端优先验证。重新 build（CSS 14.61kB）+ CloudStudio 部署成功（同沙盒覆盖）。TODO-4 标记 ✅ 已落地。

## 主题符号化 + 入场动画随主题（14:45 起）
- 用户反馈三点：① 右上角图标（原齿轮/光芒混合体，用户视作「阳光」）应对应主题——已导出 `ThemeGlyph`（SettingsPanel 内 SunIcon/LeafIcon/MoonIcon），Home 顶栏设置按钮随 theme 切换为 暖阳☀/新芽🌿/月亮🌙；② 入场动画随主题——新增 `EMBLEMS` 映射：暖黄/深黑=爱心+「将心注入·将爱注入」，浅绿=新芽(SPROUT_PATH 三段式:短茎+左右两叶)+「我们不做大事 我们只会扎根」；`EntryOverlay` 增 `theme` prop；新增 `--emblem/--emblem-shadow` 变量（暖黄/深黑=暖玫瑰 #e3a6a6，浅绿=新芽绿 #6fae55）；③ 绿色色系待商讨（未定稿，当前仍旧绿）。
- 构建踩坑：`tsc -b` 报 TS6133（测试模式注释掉 1H 逻辑后 ENTRY_KEY/ENTRY_GAP_MS 无人读）+ TS2322（theme 需 ThemeKey 类型）。修复：`void ENTRY_KEY/ENTRY_GAP_MS` 占位（恢复时随注释块删）+ SettingsPanel `export type ThemeKey` + Home `useState<ThemeKey>`。build 通过（JS 208.55kB/CSS 14.83kB）。
- 已部署（同沙盒 191d2bb… 覆盖）。**绿色配色商讨中**：已给 A 嫩芽绿(现状微调)/B 鲜亮草绿(推荐)/C 薄荷青绿 三方向色板，等用户拍板后改 App.css 绿主题变量并重部署。开场动画 1H 逻辑仍测试模式。
- **绿色定稿 B·鲜亮草绿（14:55）**：用户从三方向色板选 B。App.css 绿主题整组替换为生机草绿（--bg #eaf6e2 / --accent #4c9e3f / --emblem #5cb53e / --rank-bg #cdeea8→#8fd45e→#5fb83f 等），已 rebuild（CSS 14.83kB）+ CloudStudio 部署成功（同沙盒覆盖）。绿色主题现为：鲜亮草绿 + 新芽入场徽记 + 「我们不做大事 我们只会扎根」。

## 入场徽记三主题化 v2（15:20 起）
- 用户三点：① 叶芽要更圆润 → SPROUT_PATH 重画为「短茎 + 两片椭圆嫩叶」（`a3.2 3.7` 椭圆弧，圆鼓鼓不再尖）；② 绿色主题「进入」按钮小图标=绿色主题叶芽 → SPROUT_PATH 改为 export，SettingsPanel 的 LeafIcon 换成填色版 SproutIcon（设置卡 + ThemeGlyph 顶栏按钮 + 入场大徽记 + 进入钮四处统一同一颗圆润新芽）；③ 夜色入场换成月亮+「曾经初心依旧炙热 庆幸自己未曾退缩」→ 新增 MOON_PATH 暖金弦月（FA 风格），dark --emblem 改 #d8b46a 暖金，12 字话语加 `@media(max-width:380px)` 收字号/字距防窄屏换行。
- EMBLEMS 现为：warm=心+将心注入将爱注入 / green=圆润新芽+我们不做大事我们只会扎根 / dark=暖金弦月+曾经初心依旧炙热庆幸自己未曾退缩。rebuild（JS 208.45kB/CSS 14.93kB）+ CloudStudio 部署成功（同沙盒覆盖）。开场动画 1H 逻辑仍测试模式。

## 夜色话语两行排布 + 绿色暂缓（15:30）
- 用户反馈：夜色非常好；手机端「曾经初心依旧炙热 庆幸自己未曾退缩」单行长、换行尴尬 → 改为**刻意两行**「曾经初心依旧炙热\n庆幸自己未曾退缩」，`.entry-overlay__motto` 加 `white-space:pre-line` 让 \n 生效，窄屏 380px 收字号规则保留兜底。rebuild（CSS 14.95kB）+ CloudStudio 部署成功（同沙盒覆盖）。
- 用户明确：**绿色主题还需再改，后面再说**（本轮不碰绿色）。开场动画 1H 逻辑仍测试模式。

## 前端优化三连（17:17 起）
- ① 奶黄入场爱心改色：用户嫌粉色，`--emblem`（:root 暖黄）从暖玫瑰 #e3a6a6 改为**奶黄油色 #e2b155**（阴影 rgba(226,177,85,.5)），与奶黄色系/摩卡金一致；② 页脚话语随主题：EntryOverlay 新增导出 `getMotto(theme)`（把入场话语 \n 拼成一行），Home 页脚 `——{getMotto(theme)}——`，三主题各自呼应入场；③ 绿色叶芽用户仍嫌丑 → 不拍脑袋，先给 3 个候选（A 双叶展开 / B 单片卷叶 Material-eco / C 圆叶小芽）在对话里渲染预览让用户挑，选中后替换 SPROUT_PATH。
- ①② 已 rebuild（JS 208.50kB/CSS 14.95kB）+ CloudStudio 部署成功（同沙盒覆盖）。③ 待用户选型。
- **③ 叶芽定稿 B·单片卷叶（17:27）**：用户先选 A 后又改口要 B → SPROUT_PATH 最终替换为 **Material eco 单片卷叶**（单一闭合路径，设置卡/ThemeGlyph/入场大徽记/进入钮四处同步）。rebuild（JS 208.48kB/CSS 14.95kB）+ CloudStudio 部署成功（同沙盒覆盖）。绿色主题叶芽到此为止，绿色色系本身仍待用户后续再调。

## 「新闻垃圾桶」讨论（20:48，讨论模式不改码）
- 用户构思（对应 TODO-5 浏览垃圾桶落地方向）：**点击热点=已读=从榜上消失+第11名补位**；垃圾桶存「今天一天看过的浏览记录」，兜底误触。
- 我的评估：概念成立 =「已读即隐藏」经典模型（Reddit hide / 新闻App 不感兴趣），闭环完整。已给出流程示意图（对话内渲染）。
- 待用户拍板的关键点：① 隐藏时机=点击即消失（配撤销Toast兜底，建议）还是返回后才消失；② 是否要「已隐藏·撤销」3 秒 Toast 即时反悔通道（建议要，轻量）；③ 过滤基准=条目唯一标识（url/id）而非排名位（刷新后排名会变）；④ 还原语义=按当前 hot_value 回榜而非原位；⑤ 今日=自然日 00:00 清空；⑥ 点「⋯」气泡看全文不算已读；⑦ 方案B 仅存本机，跨设备不同步。
- **决策定稿（21:03）**：① 点击即隐藏；② **只留 3 秒撤销 Toast**，垃圾桶降级为「今日浏览记录」只存档**不提供还原**（用户选择，榜单逻辑最简）；③ 入口=设置面板预留位；④ 过滤基准=条目唯一标识（url）；⑤ 今日=自然日 00:00 重置；⑥ 气泡不算已读；⑦ **排名数字=重编号**：数字=当前可见榜序号，永远 1-10 无空号，1/2/3 金色徽章给当前可见前三，真实热度仍看卡片右侧热度值（item.heat 保留）。
- 实现备忘（下次实现模式用）：HotCard 的 `key`/`openRank` 需从 `item.rank` 换成唯一标识（url）；显示序号由「可见列表重排」计算；隐藏集合本机存储+自然日重置；「第 11 名补位」= 取满 10 条可见自动成立。

## 「新闻垃圾桶」实现落地（21:09 起）
- 数据层：`api/hot.ts` 的 `ITEM_LIMIT` 10 → **50**（uapis.cn 单平台约 50 条，卡片层过滤后取满 10 条展示，仅渲染 10 行不卡；用户确认 50 条 OK）。
- 新增 `hooks/useReadHistory.ts`：`ReadRecord{url,title,source,heat,time}`，localStorage 键 `mini-hot-hub:read-history`，存储 `{date:'YYYY-MM-DD', items[]}`，自然日自动重置；暴露 `hidden(Set)/records/hide/unhide`。
- 新增 `components/UndoToast.tsx/.css`：底部胶囊，3 秒窗口（父组件 key=url 重挂载重置计时），点「撤销」回榜。
- `HotCard.tsx`：`key`/气泡状态从 rank 换成 `identityOf(item)=url||title`；新 `ReadList` 组件=过滤已读→取满 10→重编号 1..10（永远满榜无空号）；点标题链接 `onRead` 触发（气泡不算已读）；空态「我也是有底线哒～」+ 副句；**该榜已看 ≥40 条时列表底部浮现「今日已看 N 条，我也是有底线哒～」**（.hot-card__list-end）。
- `SettingsPanel`：浏览垃圾桶落地（TODO-5 ✅）=「今日浏览记录」，点击展开列表（标题+平台+HH:mm），只存档不还原（附 hint 说明）；投喂金句仍规划中。Home 接线 hidden/records/hide/unhide + toast 状态。
- rebuild（JS 211.57kB / CSS 17.03kB）+ CloudStudio 部署成功（同沙盒覆盖）。开场动画 1H 逻辑仍测试模式。TODO-5 标记待更新。

## 首轮体验修复（21:26）
- 用户测出 4 点，全部修复并部署：
  1. **气泡 bug**：气泡元素之前没被 isTruncated 条件包住（桌面 hover 时标题没超长也弹）→ 按钮+气泡一起收进 `{isTruncated && ...}`。
  2. **撤销 Toast 取消**：点击即跳新标签页，回来 3 秒早过，功能等于摆设 → 删除 UndoToast.tsx/.css、Home toast 状态、useReadHistory 的 unhide；隐藏即当日永久（垃圾桶只存档）。
  3. **垃圾桶记录可跳转**：标题包 `<a href>`（仅 url 以 http 开头时，兜底纯文本），hover 转 accent。
  4. **记录列表位置**：从「沉在投喂金句之后」改为「以浏览垃圾桶行为顶往下排」——拆成两个 ul，记录列表直接跟在垃圾桶行下方，投喂金句移到列表之后。
- rebuild（JS 211.21kB / CSS 16.26kB，32 模块）+ CloudStudio 部署成功（同沙盒覆盖）。

## 垃圾桶提示文案改可爱（21:33）
- 用户嫌「今日浏览记录，只存档不还原；自然日 00:00 自动清空」太官方，给了 4 方向（A 不退不换/B 翻篇/C 收信不捞人/D 焕新）让选。
- 用户选 **B 翻篇** → SettingsPanel.tsx 改为「看完就翻篇啦，明天 00:00 自动清零」。
- rebuild（JS 211.18kB / CSS 16.26kB）+ CloudStudio 部署成功（同沙盒覆盖）。

## 三处小优化（21:40）
- ① 隐藏渐变：HotItem 加 exiting 状态 + setTimeout(420ms) 延迟 onRead，CSS .hot-card__item 固定 height:44px（原 ~40px，+4px 更透气），transition height 0.4s + opacity 0.3s + border-bottom-color 0.4s，is-exiting 时 height→0/opacity→0/边线透明，下方列表自然上升；overflow 保持 visible 避免裁切气泡。
- ② 垃圾桶记录归属感：.settings-records 加 var(--bg) 背景+1px solid 细边+圆角 10px+内边距 6/12/10，作为「以浏览垃圾桶为顶」的独立子单元；动画从 settings-up（自下）换成 settings-down（translateY -8px→0，从上滑出），从垃圾桶行往下弹出的感觉。
- ③ 气泡慢出现：.hot-card__bubble transition 0.18s → 0.45s（opacity/transform/visibility 同步）。
- rebuild（JS 211.39kB / CSS 16.57kB）+ CloudStudio 部署成功（同沙盒覆盖）。
