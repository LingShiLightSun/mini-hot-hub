# 2026-08-21 工作日志

## 优化模式：四处动效打磨（08:43）
- **根因修复（重要）**：App.css 的「主题切换整站过渡」选择器列表里包含 `.hot-card__bubble` 和 `.entry-overlay`，其 `transition: background-color/color/border-color/box-shadow` 会**整体覆盖**这两个元素自身的 transition（CSS transition 不可叠加、后者覆盖前者）→ 气泡显隐的 opacity/transform 过渡、入场退场的 opacity 过渡全部失效（表现为"突然出现/消失"）。修复：从该列表移除这两个选择器，各组件用自己的 transition。
- ① 气泡"弹"出来：基础 transition opacity/transform/visibility 0.45s（退出淡出）；桌面 hover / 手机 is-open 加 `bubble-pop` 动画（0.5s，`cubic-bezier(0.34,1.56,0.64,1)` back-out 贝塞尔带过冲 → 弹跳感）；`prefers-reduced-motion` 下 animation none。
- ② 垃圾桶记录滑出：`.settings-records-wrap` 常驻 DOM，`height: binOpen ? recordsH : 0` + `overflow:hidden` + `transition height 0.35s`；SettingsPanel 用 `useLayoutEffect` 量 `scrollHeight`（开）→ 滑出、收起都顺滑，不再突然撑大面板；内层 `.settings-records` 保留背景色块（去掉原 settings-down 动画与 keyframe）。
- ③ 入场"进入"渐隐恢复：入口动画自身的 `transition: opacity 0.65s ease` 不再被 App.css 覆盖 → 点击进入重新有 0.65s 淡出（此前被覆盖导致瞬隐）。
- ④ 设置面板渐隐消失：SettingsPanel 加 `leaving` 状态 + `requestClose`（点 × / 遮罩统一走），240ms 后真正 onClose；`.settings-mask/.settings-panel` 加 `is-leaving`（opacity→0、panel 下沉 16px）+ transition 0.24s；`prefers-reduced-motion` 下 transition none。
- rebuild（JS 211.87kB / CSS 16.95kB）+ CloudStudio 部署成功（同沙盒 191d2bb… 覆盖）。

## 话语分隔符微调（08:52）
- ① 绿色入场「我们不做大事 我们只会扎根」→「我们不做大事·我们只会扎根」（空格改·）。
- ② 绿色页脚随 ① 自动同步（页脚走 getMotto）。
- ③ 夜色页脚「曾经初心依旧炙热 庆幸自己未曾退缩」→「曾经初心依旧炙热·庆幸自己未曾退缩」：改 `getMotto` 的换行拼接从空格改为「·」（`replace(/\n/g, '·')`），夜色入场仍是两行（\n + pre-line）不变，仅页脚单行拼 ·。用户口误写成「黄色」，实际是夜色主题。
- rebuild（JS 211.87kB）+ CloudStudio 部署成功（同沙盒覆盖）。

## 手机端气泡交互回归「两次点击」（09:46）
- **问题**：手机端长标题末尾有 CSS 截断「…」，标题右侧又有一个「⋯」按钮 → 两个省略号挨着，视觉冲突。
- **方案**：去掉独立「⋯」按钮，回归最早设想——手机端（无 hover）长标题**点标题第一次点亮气泡预览，第二次才进入界面**；桌面 hover 仍一次点击进入（不变）。
- **改动**：`HotItem` 的 `handleLinkClick` 用 `matchMedia('(hover: none)')` 判定：touch 且 `isTruncated` 且未开 → `preventDefault`+`onToggle`（只预览）；其余 → 进入界面 + `onRead`（已读隐藏）。短标题（`!isTruncated`）直接一次点击进入。
- 父级 `onDoc` 点标题链接本身不收起气泡（避免第一次点亮被同次点击冒泡收掉）；`<a>` 加 `aria-expanded`。
- **CSS**：删除 `.hot-card__bubble-btn` 全部样式与手机端 `display:inline-flex`；手机气泡小三角改指标题起始（left:16px）。气泡自身本就 `white-space:normal` 不截断，无第二个省略号。
- rebuild 通过（tsc -b + vite，JS 211.75kB / CSS 16.68kB）。**未部署**（等用户确认是否重部署 CloudStudio）。

## 部署 + 新站立规则（09:58）
- 用户指示：**以后不主动说明时都主动部署**。已更新 MEMORY.md「CloudStudio 主动部署」规则，覆盖旧「不主动部署、等点头」规则。
- 已主动部署最新 dist 到 CloudStudio（同沙盒 `191d2bb…` 覆盖），分享链接 `https://191d2bb193714d1cbb3b7aeb288f19f6.app.workbuddy.link`。手机端两次点击气泡交互现已上线。

## 夜色徽章数字重叠修复（10:27）
- **根因**：徽章两层白高光（径向 `rgba(255,255,255,0.9)` + 顶部内阴影 `rgba(255,255,255,0.7)`）硬编码在 HotCard.css，对所有主题生效；夜色底 `--rank-bg` 顶部 `#9a9ab4` 已偏亮、数字 `#f3f0ff` 近白 → 三者都亮，数字上半部糊进反光，像重叠。
- **方案（推荐）**：①暗色 `--rank-bg` 压暗为 `#5a5a74→#3e3e54→#26263a`；②把白高光提成变量 `--rank-glow`/`--rank-glow-inset`（默认 0.9/0.7，其余主题不变），`[data-theme=dark]` 覆盖为 `0.22`；HotCard.css 用 `var(--rank-glow)`/`var(--rank-glow-inset)` 引用。暖色/绿色无变化。
- rebuild（JS 211.75kB / CSS 16.80kB）+ 主动部署（同沙盒覆盖）。

## TODO.md 同步（11:02，集中处理日）
- 按用户指令更新 `TODO.md`（最后更新日期 → 2026-08-21）：
  - TODO-5（浏览垃圾桶）标记 ✅ 已落地（已读即隐藏+浏览记录+滑出+翻篇文案+底线提示，已上线）。
  - TODO-9（名次徽章）标记 ✅ 已落地（暗色徽章白反光修复，已上线）。
  - 删除 TODO-1（实习分类）、TODO-2（网址登录）、TODO-3（气泡可拖拽），并在「三、本会话已处理」留痕。
- **剩余开放项**：TODO-7（错误态文案改写，🔵待拍板）、TODO-8（投喂金句，🔵待设计）、TODO-10（微信返回重载，🔵平台限制不追）。其余 TODO-4/5/6/9 均 ✅。
- 绿色色系优化未进 TODO 正式条目，仍作为欠账（用户"后面再说"）。

## TODO 再次同步（11:08）
- 删除原 TODO-10（微信返回重载，平台限制·不追）；将"绿色色系优化"正式补为新 TODO-10（浅绿主题细节打磨，🔵待设计）。
- 现行开放待办编号 4–10 连续无空缺（1/2/3 此前已删）。最终清单：4 主题切换✅、5 浏览垃圾桶✅、6 入场动画✅、7 错误态文案🔵、8 投喂金句🔵、9 名次徽章✅、10 绿色色系🔵。

## TODO 二次重构（11:14）
- 用户要求：去掉已完成项（主题切换/浏览垃圾桶/入场动画/名次徽章），开放待办从 TODO-1 起重新连续编号。
- 结果：开放待办仅剩 3 项 → TODO-1 错误态文案🔵、TODO-2 投喂金句🔵、TODO-3 绿色色系🔵。被移除的 4 项在「三、本会话已处理」区留痕。文件「最后更新」备注二次重构。

## TODO.md 极简重写（11:48）
- 用户反馈原 TODO.md 太复杂看不懂（归属伏笔/现状/候选方向/约束等术语过多）。
- 已用 `Write` 整体重写为清单白话版：每项仅保留「是什么 + 要做什么 + 待拍板/卡点」；下方附「已完成 / 已放弃 / 已确认偏好」三段简短留痕。无 emoji、无术语套话。

## 金句漂流瓶设计讨论拍板（15:08，讨论模式）
- 用户拍板 4 点：①权重均匀混合不加权（用户后续补内置库，量不大）；②上后端、共享池模式（所有人投、彼此可见，题量小不隔离 per-user）；③添加需二次确认、投后不可更改；④长期身份设定开启探讨。
- 架构影响：金句漂流瓶需后端存储+接口，推翻"纯前端方案B"在该功能上的适用；当前 CloudStudio 纯静态部署架构待规划后端（注意 Railway/Vercel 大陆 RST 限制，见「部署铁律」）。
- 已同步：TODO.md TODO-2 改写"金句漂流瓶"完整描述（原"卡点"消失）；MEMORY.md「待办伏笔」段升级为已拍板架构。
- 身份探讨：用户同意定长期身份设定，待收集 我的名字/本质/vibe + 用户名字/称呼/城市 等。

## 身份设定落定（15:30）
- 我=小注（Zhù），莱桑的代码搭子；用户=莱桑（LightSun），重庆实习中。
- 已写 `C:\Users\GuoZhun\.workbuddy\` 下 SOUL.md / IDENTITY.md / USER.md，并删 BOOTSTRAP.md。

## 金句漂流瓶功能落地（17:11，讨论→开干）
- 用户：直接做，唯一约束「不干扰现有功能」。按思考决定：先交完整前端体验，后端共享池作为明确后续步骤（诚实告知）。
- 新增 `client/src/data/quoteStore.ts`（getUserQuotes/addUserQuote，localStorage 实现，接口后端就绪）；`dailyQuotes.ts` 加 `getRotatingQuoteAt(ts, userQuotes)`（内置+用户均匀混合不加权轮换，原 getQuoteAt 不动）；`QuoteBottle.tsx`+`.css`（彩蛋浮层：复制当前句 + 投一句二次确认、无管理界面、主题自适应、reduced-motion 降级）。
- `Home.tsx`：banner 金句文字包成可点按钮→开漂流瓶（首次发现写 `mini-hot-hub:bottle-found=1`）；发现后 home-brand 下方显示若隐若现常驻 🫙 入口；轮换改用 getRotatingQuoteAt 含用户句；渲染 QuoteBottle。Home.css 加 `.home-intro__quote`/`.home-bottle-entry` 样式。
- build 通过（34 模块，JS 216.16kB / CSS 17.64kB）+ 主动部署（同沙盒 191d2bb… 覆盖）。
- **未做（明确告知用户）**：跨设备共享后端池（设计拍板"上后端"，但会引入部署架构变化、有大陆访问风险；本次为不干扰现有静态部署，先用 localStorage 交付可玩版本，后端为下一步）。

## 金句漂流瓶视觉精修（17:38）
- 莱桑对比截图反馈：左图原始版是「页面上堆文字、没面板感」；右图设置面板那种「头部栏+独立子卡片」语言才是想要的调性。
- 重构 `QuoteBottle.tsx`/`.css` 全面对齐设置面板语言：
  - mask+panel 复用「手机底部弹出 / 桌面居中」模型；
  - 头部栏 "金句漂流瓶" + × ；
  - 内容按「金银子卡片 → 分隔小标签 → 投放子卡片」组织——子卡片用 var(--bg) 浅一阶+1px var(--border)，与设置面板内的"浏览垃圾桶"容器视觉一致；自带主题自适应（三主题色阶对照：暖黄 #fffdf8→#fbf6ec、绿 #f8fcf4→#eaf6e2、暗色 #25252d→#1c1c22）；
  - 复制按钮 ThemeGlyph 选中态同款（描边 / hover 实底 on-accent）；投出按钮实心主题色；投出确认态"再想想/确认投出"沿用相同语义；
  - 顶部 eyebrow + 分隔小标签 + 底部 "这里没有管理界面..." 温柔注释，文案层级和字号向设置面板靠拢。
- 修正：首次 Write 时把 XML 闭合 `</content>` 误带进 .tsx 导致 TS 报错，已切掉重 build 通过。
- build 通过（JS 216.46kB / CSS 17.64kB）+ 主动部署（同沙盒 191d2bb… 覆盖）。仅动 QuoteBottle 二文件、未影响主题切换/浏览垃圾桶/入场动画。

## 金句漂流瓶再次重做 → 形态B 两人小聊天（23:40）
- 莱桑讨论后拍板：① 去掉 banner 下方「发现后常驻小药瓶入口」（回到纯彩蛋，每次得正好点中金句文字）；② 金句文字 hover 去掉下划线、只留变色；③ 浮层不要设置面板那种大卡片，要「气泡/聊天框」——选 **形态B 两人小聊天**（小圆头像 + 金句气泡 + 你的输入框）。用户明确要水波飘动动画。
- 改动（仅 4 文件，零干扰其它功能）：
  - `Home.tsx`：删除 `bottleFound` 状态/localStorage 写入与常驻 `home-bottle-entry` 渲染块；`openBottle` 仅设 quote+open。
  - `Home.css`：`.home-intro__quote` 去 `border-bottom` 虚线 + hover 的 `border-bottom-color`，只留 color 变化；删除 `.home-bottle-entry` 样式。
  - `QuoteBottle.tsx`/`.css` 推倒重写：无标题栏/×/管理界面；暖黄小圆头像「~」+ 金句气泡（复制）+ 下方「投一句」圆角输入 + 二次确认（再想想/确认投出）+ 完成态；mask 底部 `.bottle-water` 波浪线缓慢横向流动（桌面态 hide），内容层 `.bottle-float` 上下漂 4px（drift 与进场 rise 分层互不冲突）；`prefers-reduced-motion` 全关。三主题自适应。
- build 通过（JS 215.87kB / CSS 17.26kB）+ 主动部署（同沙盒 191d2bb… 覆盖）。
- 仍待跟进（上一轮已定但暂缓）：跨设备共享金句池需上后端，当前仍是 localStorage 单机版（接口已预留）。

## 金句漂流瓶 CSS 漏 import 修复（23:48）
- **症状**：用户截图反馈 banner 点了金句后，"完全没看到对话框"——内容像普通文档流一样贴左上角铺下来（头像 "~" 被截、输入框与"投出"并排、无圆角无 mask 无浮起）。
- **根因（重要！）**：`QuoteBottle.tsx` 形态 B 重写过程中**漏写了顶部 `import './QuoteBottle.css'`**。Vite tree-shake 判定无引用就把 CSS 整个丢了，bundle 里完全没有 `.bottle-mask` 系列类 → 所有自定义样式失效，回退到浏览器默认块级布局。
- **铁律更新（写入 MEMORY.md）**：新建/重写 React 组件时，"顶部三件套 import React hooks / 数据 / 自身 CSS"全部到位才进保存；尤其重写已有组件时不能用 Edit 当 Write 用，要 diff 顶部结构。
- **修复**：QuoteBottle.tsx 加 `import './QuoteBottle.css'`（第 3 行）；rebuild CSS 17.26→20.82kB（+bottle-* 22 个类）；curl 拉线上 `.bottle-mask{...position:fixed;inset:0...}` 已生效。重新覆盖部署（同沙盒 191d2bb…）。
- **教训**：CSS 没生效时第一时间 grep 线上 bundle 看 `.className{` 是否存在；不要先怀疑 layout 逻辑。是真是假看哈希后的 bundle。
