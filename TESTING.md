# 测试清单（迷你今日热榜）

> 这是一份**检查记录本**：由你自己在本地逐条验证，测完告诉 AI，AI 负责把结果打勾记录在这里。
> 「怎么测 / 调试方法」仅供参考，不会替你预先下结论。
> 遇到不会测的条目，直接问 AI，AI 只教方法、不动手替你测。

图例：⬜ 待你验证　✅ 已通过　⚠️ 有问题（附说明）　❌ 未通过

---

## 一、怎么测（参考，不替你做）

| 工具 | 看什么 | 典型用途 |
| --- | --- | --- |
| 浏览器 DevTools | Network（请求是否发出、状态码、响应体）、Console（报错堆栈）、Elements（响应式布局） | 前端表现验证 |
| 终端 `curl` | 后端接口真实返回 | 隔离「前端问题」还是「后端问题」 |
| 故意制造失败 | 后端 `Ctrl+C`、坏环境变量、浏览器 `?refresh=1` | 验证容错 / 缓存 |

**调试三板斧（顺序：先看网络，再看控制台，最后看代码）**
1. **看 Network**：请求发出没？状态码 200 还是 404/500？响应体 `error` 字段是否为 `true`？
2. **看 Console**：有没有红色报错？堆栈指向哪行？
3. **看代码**：从表现反推——白屏→渲染抛未捕获异常；整卡红→`error` 字段；布局乱→CSS 被覆盖；时间乱跳→缓存被重写。

---

## 二、测试清单（9 条，状态由你测完填写）

### ✅ 1. 首页三卡均显示数据
- **怎么测**：启动前后端，浏览器开 `http://localhost:5173`。
- **预期**：三张卡片（微博/知乎/B站）各显示 10 条真实热搜，标题非空、不重叠。
- **调试方法**：某卡空白→看 Network 对应 `/api/hot/:source` 是否 200；有 `error:true` 看 `message`；三卡全空→`curl http://localhost:3001/api/hot/weibo` 验后端是否起。

### ✅ 2. 每条标题可点击跳转
- **怎么测**：点任意一条标题。
- **预期**：新标签页打开 `item.url`（微博详情/知乎问题/B站视频）。
- **调试方法**：点不动/打不开→查 `item.url` 是否为空（缺失时 service 会用平台搜索页兜底）。

### ✅ 3. 排名、热度显示正确
- **怎么测**：看每张卡第一条。
- **预期**：排名 `1,2,3…` 连续；热度显示 `xxx万`/`x.xx亿`；前三名金/银/铜圆标。
- **调试方法**：排名错乱→查 service 的 `index/rank` 映射；热度空白→`heat` 为 `undefined` 或 `formatHeat` 返回空（B站源需先剥「播放」后缀）。
- **2026-08-18 修复记录**：你反馈「知乎没热度，微博/B站有」。根因：uapis.cn 知乎源 `hot_value` 是 `"1602 万热度"` 这种**带空格+中文后缀**的字符串，旧 `formatHeat` 直接 `Number(value)` 得到 `NaN` → 返回空串。已重写为 robust 版（先剥非数字字符，再判断原串是否已带"万/亿"单位），同时支持纯数字（微博）、带空格热度（知乎）、带"播放"（B站）、已格式化万串四种格式。**进一步抽成 `server/utils/formatHeat.js` 共享模块，三个 service 改为 `import { formatHeat }`，行为完全一致、单一维护点**（新增平台直接 import 即可）。三平台排序策略经讨论确认为「统一按 `hot_value` 降序」（详见 README「排序方式与热度语义」一节）：微博/知乎 `hot_value` 即官方综合热度分、与官方序一致；B 站 `hot_value` 为累计播放量、排得播放量榜。本条 ✅。

### ✅ 4. 更新时间文案合理
- **怎么测**：看卡片底部。
- **预期**：显示「刚刚更新」/「更新于 X 分钟前」/「更新于 X 小时前」/「更新于 X 天前」/「更新于 MM-DD」。
- **调试方法**：显示原始 ISO→`updatedAt` 缺失或非日期；显示「—」→加载态或无数据。缓存期内文案不变属正常。

### ✅ 5. 单平台失败时其他正常
- **怎么测**：用坏地址起后端再刷新首页：
  ```powershell
  cmd /c "set ZHIHU_API_URL=http://127.0.0.1:9/zhihu && node --watch index.js"
  ```
- **预期**：该平台卡红色错误态+重试按钮，其余两卡正常，整包 HTTP 200 不崩。
- **调试方法**：全崩→查 `getPlatformData` 是否异常泄漏到 reject；前端是否误用全局 `error`。

### ✅ 6. 缓存期内 updatedAt 不乱跳
- **怎么测**：不点刷新、不传 `refresh=1`，连续多次刷新首页（间隔 < 600 秒）。
- **预期**：每卡底部 `updatedAt` 文案保持一致（相对时间随真实流逝缓慢变化，但 `updatedAt` 源头不变）。
- **调试方法**：乱跳→是否误传 `?refresh=1`（那会强抓、更新属正常）；或后端是否每次请求都 `setCache`（应只在 miss 时写）。

### ✅ 7. 手机宽度布局正常
- **怎么测**：DevTools 切响应式/手机尺寸（如 375px），或窗口缩到 < 720px；真机用 `http://<局域网IP>:5174` 打开（Vite `--host`，见 MEMORY「真机调试」）。
- **预期**：三卡变单列纵向、不重叠、可上下滚动；标题过长**自动换行显示完整**（今日头条式，`white-space:normal`），整页**不再横向滑动**；桌面端（≥720px）仍维持单行 `…` 省略。
- **调试方法**：窄屏仍是三列→media query 被覆盖；重叠→查 `grid gap`；真机标题可横滑→原 flex 子项 `text-overflow:ellipsis` 在移动端失效，已改为自动换行（`HotCard.css` 手机端 `white-space:normal` + `overflow-wrap:anywhere`，桌面端 `@media(min-width:720px)` 覆盖回单行省略）。

### ✅ 8. 页脚合规文案存在
- **怎么测**：滚到首页底部。
- **预期**：看到「学习项目 · 仅供学习交流 · 非商用」+ 数据来源/版权/免责声明。
- **调试方法**：找不到页脚→`Home.tsx` 的 `<footer>` 是否被删；文案不符→改 `.home-footer__line`。
- **备注**：2026-08-18 已把过期「（当前为 Mock 数据）」改为真实来源说明，用户复核文案合规、拍板通过。

### ✅ 9. 硬刷新、后端挂掉时前端不白屏
- **怎么测 A**：`Ctrl+Shift+R` 硬刷新 → 正常重载。
- **怎么测 B**：停掉后端（`Ctrl+C`），前端硬刷新或点「刷新」。
- **预期**：页面不白屏；三卡红色错误态「无法连接后端…」+ 重试按钮；顶栏/页脚仍在。
- **调试方法**：白屏→看 Console 是否有 React 渲染未捕获异常；检查是否某处直接 `throw` 而非走 `error` 态。
- **2026-08-18 验证记录**：后端（3001）停掉后前端（5173）显示三卡红色错误态「后端返回错误（HTTP 502），请稍后重试」+「点击重试」按钮，顶栏/页脚均在，**无白屏**，符合预期。

---

## 三、你的检查记录（AI 代填）

| # | 结果 | 你的实际现象 / 备注 | 日期 |
| --- | --- | --- | --- |
| 1 | ✅ | 已通过 | 2026-08-18 |
| 2 | ✅ | 已通过 | 2026-08-18 |
| 3 | ✅ | 已通过。讨论确认：三平台统一按 `hot_value` 降序排（微博/知乎=官方热搜序、B站=累计播放量榜），排序与热度语义均正确 | 2026-08-18 |
| 4 | ✅ | 已通过。截图实测三卡片底部均显示「刚刚更新」，文案符合预期 | 2026-08-18 |
| 5 | ✅ | 已通过。截图：知乎卡「获取失败+点击重试」按钮、微博/B站正常，整页未白屏。**前置排查**：测前必须 `taskkill` 干净旧 3001 server 否则新坏地址命令因端口被占静默不上 | 2026-08-18 |
| 6 | ✅ | 已通过。代码证据：`updatedAt` 在 cache miss 时 `new Date()` 生成并冻结进缓存，cache hit 原样返回，缓存期内不变；前端「X分钟前」为相对时间自然递增属正常，不等于 updatedAt 乱跳 | 2026-08-18 |
| 7 | ✅ | 已通过。手机实测：三卡单列纵向、标题自动换行显示完整（今日头条式）、整页纯上下滑不再横向滑动；桌面端仍单行省略（移动端换行已确认为正式方案） | 2026-08-18 |
| 8 | ✅ | 已通过。用户复核页脚合规文案（学习项目·非商用 + 真实数据来源/免责声明），原过期「Mock 数据」字样此前已更正 | 2026-08-18 |
| 9 | ✅ | 已通过。后端停掉后 5173 显示三卡红错误态「HTTP 502…」+ 点击重试，顶栏/页脚在，无白屏 | 2026-08-18 |
