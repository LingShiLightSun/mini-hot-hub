# 4.AGENTS

# Vibe Coding 热点网页 · 开发指令（AGENTS.md）

> 视角：MVP 纯前端；后端为后续扩展，本期不做

## 项目概述

使用 React + TypeScript + Vite + CSS 开发纯前端热点聚合网页，一个页面浏览微博 / 知乎 / B 站等平台当下热度最高的事件。

MVP 不引入后端；后续可选 Node.js + Express 扩展（约定见「后续后端扩展」小节）。

## 开发规范

* 使用 TypeScript，前端类型与 `tech_design.md` 的 `HotItem` / `HotPlatform` 保持一致
* 使用函数式组件 + Hooks
* 样式使用纯 CSS + CSS 变量（按组件拆分文件），保持简洁
* 组件可复用：HotCard、HotList、Layout、TabBar、Settings

## 代码风格

* 组件名 PascalCase，函数 camelCase
* 数据只通过聚合 API 获取：fetch `VITE_API_BASE` 指向的聚合 API 域名
* **禁止在前端 fetch 微博 / 知乎 / B 站等平台原始域名**（CORS 限制 + 规避风险）
* Mock 模式：未配置 API 时读取 `src/mock/` 本地 JSON

## 设计要求

* 视觉基调：极简、清新、治愈「罗小黑战记」风，最喜欢的角色自然融入，不显幼稚
* 参考今日热榜的信息密度，清爽易读
* 桌面 3 列卡片，移动端 1 列（响应式，无横向滚动）
* 排名 1～3 可视觉强调（如配色 / 图标）
* 单卡失败显示错误文案，不拖垮整页

## 注意事项

* 数据源走免费聚合 API（CORS 已解决），具体域名 / 路径动手前实测连通性
* 前端内存缓存 TTL 默认 600 秒，可用环境变量（如 `VITE_CACHE_TTL`）配置
* 不要把敏感信息提交到公开 GitHub
* 页脚注明：学习项目、非商用、数据来源

## 测试要求

* 每完成一个平台，手动验证 10 条数据正确渲染
* 测试：单平台挂掉时其他平台仍正常
* 测试：10 分钟内重复刷新不会疯狂打上游（命中缓存）

## 后续后端扩展（MVP 不做，预留约定）

* 技术：Node.js + Express
* 接口路径：`/api/hot/:source`（source = weibo | zhihu | bilibili），可选 `/api/hot` 一次返回全部
* 上游请求加合理 User-Agent、Referer（按平台文档）
* 缓存 TTL 默认 600 秒，可用环境变量 `CACHE_TTL` 配置
* 开发期 Vite 将 `/api` 代理到 `http://localhost:3001`
